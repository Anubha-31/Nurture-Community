---
- hosts: test
  become: True
  tasks:
    - name: Start the container
      docker_container:
        name: nodetest
        image: "cranifax/nodetest:{{DOCKER_TAG}}"
        state: started
        published_ports:
          - 40.71.214.107:8060:80
